{
  "enabled": {
    "description": "是否启用动态人格插件",
    "type": "bool",
    "default": true
  },
  "session_filter_mode": {
    "description": "会话过滤模式",
    "type": "string",
    "default": "disabled",
    "hint": "disabled：所有会话均生效；whitelist：仅在列表中的会话生效；blacklist：列表中的会话不生效",
    "options": ["disabled", "whitelist", "blacklist"]
  },
  "session_filter_list": {
    "description": "会话过滤列表（群号 / 用户 ID）",
    "type": "list",
    "hint": "填写群号（group_id）或私聊用户 ID。白名单模式下只有列表中的会话启用插件，黑名单模式下列表中的会话禁用插件。发送 /dp sessionid 可查看当前会话 ID",
    "default": []
  },
  "selector_provider_id": {
    "description": "Selector LLM 使用的模型提供商（留空则使用会话当前模型）",
    "type": "string",
    "_special": "select_provider",
    "hint": "建议选择一个轻量、快速的模型（如 qwen-turbo）以降低延迟，留空则与主对话使用同一模型",
    "default": ""
  },
  "inject_mode": {
    "description": "人格注入方式",
    "type": "string",
    "default": "replace",
    "hint": "replace：完全替换 system_prompt（推荐）；prepend：将选中人格追加到原有 system_prompt 之前",
    "options": ["replace", "prepend"]
  },
  "cache_ttl": {
    "description": "Selector 结果缓存消息条数（同一会话中复用 N 条后才重新选择人格）",
    "type": "int",
    "default": 3,
    "hint": "设为 0 则每次消息都重新调用 Selector LLM（精准但有额外延迟）"
  },
  "selector_prompt_extra": {
    "description": "Selector 附加提示词（追加到 Selector 系统提示末尾，可用于微调选择偏好）",
    "type": "text",
    "default": "",
    "hint": "例如：当用户使用中文时，优先选择更活泼的人格"
  },
  "persona_rules": {
    "description": "动态人格规则列表（至少启用 2 个人格插件才会生效）",
    "type": "template_list",
    "hint": "从 AstrBot 人格列表中选择人格，为每个人格填写描述和适用场景，并决定是否启用",
    "templates": {
      "persona_rule": {
        "name": "人格规则",
        "hint": "配置一条人格规则：选择人格、填写描述、设定使用场景",
        "items": {
          "rule_enabled": {
            "description": "是否启用该人格规则",
            "type": "bool",
            "default": true
          },
          "persona_id": {
            "description": "选择人格",
            "type": "string",
            "_special": "select_persona",
            "hint": "从 AstrBot WebUI 人格管理页面中已创建的人格列表中选择"
          },
          "provider_id": {
            "description": "该人格使用的模型提供商（留空则使用当前会话默认模型）",
            "type": "string",
            "_special": "select_provider",
            "hint": "为该人格指定专用模型。例如：角色扮演人格用创意模型，严肃助手人格用推理模型。留空则跟随当前会话",
            "default": ""
          },
          "persona_desc": {
            "description": "人格描述（该人格的身份与能力）",
            "type": "text",
            "hint": "描述该人格的角色定位和能力特征。例如：专业的计算机领域从业者，具有调用本机 shell 的能力，擅长代码调试与系统管理",
            "default": ""
          },
          "scenario_desc": {
            "description": "使用该人格的情况（何时应该切换到此人格）",
            "type": "text",
            "hint": "描述什么样的用户消息应该触发切换到此人格。例如：用户发出相对严肃的任务请求、知识询问或命令指示时启用该人格",
            "default": ""
          }
        }
      }
    }
  }
}
